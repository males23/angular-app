import { ServiceTypeColorPipe } from './service/service-type-color.pipe';

describe('ServiceTypeColorPipe', () => {
  it('create an instance', () => {
    const pipe = new ServiceTypeColorPipe();
    expect(pipe).toBeTruthy();
  });
});
